// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/DouDOU-start/airgate-core/ent/opswindowstat"
	"github.com/DouDOU-start/airgate-core/ent/predicate"
)

// OpsWindowStatQuery is the builder for querying OpsWindowStat entities.
type OpsWindowStatQuery struct {
	config
	ctx        *QueryContext
	order      []opswindowstat.OrderOption
	inters     []Interceptor
	predicates []predicate.OpsWindowStat
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OpsWindowStatQuery builder.
func (owsq *OpsWindowStatQuery) Where(ps ...predicate.OpsWindowStat) *OpsWindowStatQuery {
	owsq.predicates = append(owsq.predicates, ps...)
	return owsq
}

// Limit the number of records to be returned by this query.
func (owsq *OpsWindowStatQuery) Limit(limit int) *OpsWindowStatQuery {
	owsq.ctx.Limit = &limit
	return owsq
}

// Offset to start from.
func (owsq *OpsWindowStatQuery) Offset(offset int) *OpsWindowStatQuery {
	owsq.ctx.Offset = &offset
	return owsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (owsq *OpsWindowStatQuery) Unique(unique bool) *OpsWindowStatQuery {
	owsq.ctx.Unique = &unique
	return owsq
}

// Order specifies how the records should be ordered.
func (owsq *OpsWindowStatQuery) Order(o ...opswindowstat.OrderOption) *OpsWindowStatQuery {
	owsq.order = append(owsq.order, o...)
	return owsq
}

// First returns the first OpsWindowStat entity from the query.
// Returns a *NotFoundError when no OpsWindowStat was found.
func (owsq *OpsWindowStatQuery) First(ctx context.Context) (*OpsWindowStat, error) {
	nodes, err := owsq.Limit(1).All(setContextOp(ctx, owsq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{opswindowstat.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (owsq *OpsWindowStatQuery) FirstX(ctx context.Context) *OpsWindowStat {
	node, err := owsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first OpsWindowStat ID from the query.
// Returns a *NotFoundError when no OpsWindowStat ID was found.
func (owsq *OpsWindowStatQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = owsq.Limit(1).IDs(setContextOp(ctx, owsq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{opswindowstat.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (owsq *OpsWindowStatQuery) FirstIDX(ctx context.Context) int {
	id, err := owsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single OpsWindowStat entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one OpsWindowStat entity is found.
// Returns a *NotFoundError when no OpsWindowStat entities are found.
func (owsq *OpsWindowStatQuery) Only(ctx context.Context) (*OpsWindowStat, error) {
	nodes, err := owsq.Limit(2).All(setContextOp(ctx, owsq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{opswindowstat.Label}
	default:
		return nil, &NotSingularError{opswindowstat.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (owsq *OpsWindowStatQuery) OnlyX(ctx context.Context) *OpsWindowStat {
	node, err := owsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only OpsWindowStat ID in the query.
// Returns a *NotSingularError when more than one OpsWindowStat ID is found.
// Returns a *NotFoundError when no entities are found.
func (owsq *OpsWindowStatQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = owsq.Limit(2).IDs(setContextOp(ctx, owsq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{opswindowstat.Label}
	default:
		err = &NotSingularError{opswindowstat.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (owsq *OpsWindowStatQuery) OnlyIDX(ctx context.Context) int {
	id, err := owsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OpsWindowStats.
func (owsq *OpsWindowStatQuery) All(ctx context.Context) ([]*OpsWindowStat, error) {
	ctx = setContextOp(ctx, owsq.ctx, "All")
	if err := owsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*OpsWindowStat, *OpsWindowStatQuery]()
	return withInterceptors[[]*OpsWindowStat](ctx, owsq, qr, owsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (owsq *OpsWindowStatQuery) AllX(ctx context.Context) []*OpsWindowStat {
	nodes, err := owsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of OpsWindowStat IDs.
func (owsq *OpsWindowStatQuery) IDs(ctx context.Context) (ids []int, err error) {
	if owsq.ctx.Unique == nil && owsq.path != nil {
		owsq.Unique(true)
	}
	ctx = setContextOp(ctx, owsq.ctx, "IDs")
	if err = owsq.Select(opswindowstat.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (owsq *OpsWindowStatQuery) IDsX(ctx context.Context) []int {
	ids, err := owsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (owsq *OpsWindowStatQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, owsq.ctx, "Count")
	if err := owsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, owsq, querierCount[*OpsWindowStatQuery](), owsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (owsq *OpsWindowStatQuery) CountX(ctx context.Context) int {
	count, err := owsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (owsq *OpsWindowStatQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, owsq.ctx, "Exist")
	switch _, err := owsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (owsq *OpsWindowStatQuery) ExistX(ctx context.Context) bool {
	exist, err := owsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OpsWindowStatQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (owsq *OpsWindowStatQuery) Clone() *OpsWindowStatQuery {
	if owsq == nil {
		return nil
	}
	return &OpsWindowStatQuery{
		config:     owsq.config,
		ctx:        owsq.ctx.Clone(),
		order:      append([]opswindowstat.OrderOption{}, owsq.order...),
		inters:     append([]Interceptor{}, owsq.inters...),
		predicates: append([]predicate.OpsWindowStat{}, owsq.predicates...),
		// clone intermediate query.
		sql:  owsq.sql.Clone(),
		path: owsq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		WindowStart time.Time `json:"window_start,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.OpsWindowStat.Query().
//		GroupBy(opswindowstat.FieldWindowStart).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (owsq *OpsWindowStatQuery) GroupBy(field string, fields ...string) *OpsWindowStatGroupBy {
	owsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &OpsWindowStatGroupBy{build: owsq}
	grbuild.flds = &owsq.ctx.Fields
	grbuild.label = opswindowstat.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		WindowStart time.Time `json:"window_start,omitempty"`
//	}
//
//	client.OpsWindowStat.Query().
//		Select(opswindowstat.FieldWindowStart).
//		Scan(ctx, &v)
func (owsq *OpsWindowStatQuery) Select(fields ...string) *OpsWindowStatSelect {
	owsq.ctx.Fields = append(owsq.ctx.Fields, fields...)
	sbuild := &OpsWindowStatSelect{OpsWindowStatQuery: owsq}
	sbuild.label = opswindowstat.Label
	sbuild.flds, sbuild.scan = &owsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a OpsWindowStatSelect configured with the given aggregations.
func (owsq *OpsWindowStatQuery) Aggregate(fns ...AggregateFunc) *OpsWindowStatSelect {
	return owsq.Select().Aggregate(fns...)
}

func (owsq *OpsWindowStatQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range owsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, owsq); err != nil {
				return err
			}
		}
	}
	for _, f := range owsq.ctx.Fields {
		if !opswindowstat.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if owsq.path != nil {
		prev, err := owsq.path(ctx)
		if err != nil {
			return err
		}
		owsq.sql = prev
	}
	return nil
}

func (owsq *OpsWindowStatQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*OpsWindowStat, error) {
	var (
		nodes = []*OpsWindowStat{}
		_spec = owsq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*OpsWindowStat).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &OpsWindowStat{config: owsq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, owsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (owsq *OpsWindowStatQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := owsq.querySpec()
	_spec.Node.Columns = owsq.ctx.Fields
	if len(owsq.ctx.Fields) > 0 {
		_spec.Unique = owsq.ctx.Unique != nil && *owsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, owsq.driver, _spec)
}

func (owsq *OpsWindowStatQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(opswindowstat.Table, opswindowstat.Columns, sqlgraph.NewFieldSpec(opswindowstat.FieldID, field.TypeInt))
	_spec.From = owsq.sql
	if unique := owsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if owsq.path != nil {
		_spec.Unique = true
	}
	if fields := owsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, opswindowstat.FieldID)
		for i := range fields {
			if fields[i] != opswindowstat.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := owsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := owsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := owsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := owsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (owsq *OpsWindowStatQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(owsq.driver.Dialect())
	t1 := builder.Table(opswindowstat.Table)
	columns := owsq.ctx.Fields
	if len(columns) == 0 {
		columns = opswindowstat.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if owsq.sql != nil {
		selector = owsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if owsq.ctx.Unique != nil && *owsq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range owsq.predicates {
		p(selector)
	}
	for _, p := range owsq.order {
		p(selector)
	}
	if offset := owsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := owsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// OpsWindowStatGroupBy is the group-by builder for OpsWindowStat entities.
type OpsWindowStatGroupBy struct {
	selector
	build *OpsWindowStatQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (owsgb *OpsWindowStatGroupBy) Aggregate(fns ...AggregateFunc) *OpsWindowStatGroupBy {
	owsgb.fns = append(owsgb.fns, fns...)
	return owsgb
}

// Scan applies the selector query and scans the result into the given value.
func (owsgb *OpsWindowStatGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, owsgb.build.ctx, "GroupBy")
	if err := owsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OpsWindowStatQuery, *OpsWindowStatGroupBy](ctx, owsgb.build, owsgb, owsgb.build.inters, v)
}

func (owsgb *OpsWindowStatGroupBy) sqlScan(ctx context.Context, root *OpsWindowStatQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(owsgb.fns))
	for _, fn := range owsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*owsgb.flds)+len(owsgb.fns))
		for _, f := range *owsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*owsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := owsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// OpsWindowStatSelect is the builder for selecting fields of OpsWindowStat entities.
type OpsWindowStatSelect struct {
	*OpsWindowStatQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (owss *OpsWindowStatSelect) Aggregate(fns ...AggregateFunc) *OpsWindowStatSelect {
	owss.fns = append(owss.fns, fns...)
	return owss
}

// Scan applies the selector query and scans the result into the given value.
func (owss *OpsWindowStatSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, owss.ctx, "Select")
	if err := owss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OpsWindowStatQuery, *OpsWindowStatSelect](ctx, owss.OpsWindowStatQuery, owss, owss.inters, v)
}

func (owss *OpsWindowStatSelect) sqlScan(ctx context.Context, root *OpsWindowStatQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(owss.fns))
	for _, fn := range owss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*owss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := owss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
