// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/DouDOU-start/airgate-core/ent/moderationlog"
)

// ModerationLog is the model entity for the ModerationLog schema.
type ModerationLog struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 请求追踪 ID
	RequestID string `json:"request_id,omitempty"`
	// 命中用户快照（不挂外键）
	UserIDSnapshot int `json:"user_id_snapshot,omitempty"`
	// Platform holds the value of the "platform" field.
	Platform string `json:"platform,omitempty"`
	// 请求端点，如 /v1/chat/completions
	Endpoint string `json:"endpoint,omitempty"`
	// 审核模式快照：observe / pre_block
	Mode string `json:"mode,omitempty"`
	// 是否命中
	Flagged bool `json:"flagged,omitempty"`
	// 命中来源：keyword / api
	Source string `json:"source,omitempty"`
	// 命中分类（keyword 命中为 keyword）
	Category string `json:"category,omitempty"`
	// API 命中分值（keyword 命中为 1）
	Score float64 `json:"score,omitempty"`
	// 脱敏并截断后的待审文本摘录
	Excerpt string `json:"excerpt,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ModerationLog) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case moderationlog.FieldFlagged:
			values[i] = new(sql.NullBool)
		case moderationlog.FieldScore:
			values[i] = new(sql.NullFloat64)
		case moderationlog.FieldID, moderationlog.FieldUserIDSnapshot:
			values[i] = new(sql.NullInt64)
		case moderationlog.FieldRequestID, moderationlog.FieldPlatform, moderationlog.FieldEndpoint, moderationlog.FieldMode, moderationlog.FieldSource, moderationlog.FieldCategory, moderationlog.FieldExcerpt:
			values[i] = new(sql.NullString)
		case moderationlog.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ModerationLog fields.
func (ml *ModerationLog) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case moderationlog.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ml.ID = int(value.Int64)
		case moderationlog.FieldRequestID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field request_id", values[i])
			} else if value.Valid {
				ml.RequestID = value.String
			}
		case moderationlog.FieldUserIDSnapshot:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id_snapshot", values[i])
			} else if value.Valid {
				ml.UserIDSnapshot = int(value.Int64)
			}
		case moderationlog.FieldPlatform:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field platform", values[i])
			} else if value.Valid {
				ml.Platform = value.String
			}
		case moderationlog.FieldEndpoint:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field endpoint", values[i])
			} else if value.Valid {
				ml.Endpoint = value.String
			}
		case moderationlog.FieldMode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field mode", values[i])
			} else if value.Valid {
				ml.Mode = value.String
			}
		case moderationlog.FieldFlagged:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field flagged", values[i])
			} else if value.Valid {
				ml.Flagged = value.Bool
			}
		case moderationlog.FieldSource:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field source", values[i])
			} else if value.Valid {
				ml.Source = value.String
			}
		case moderationlog.FieldCategory:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field category", values[i])
			} else if value.Valid {
				ml.Category = value.String
			}
		case moderationlog.FieldScore:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field score", values[i])
			} else if value.Valid {
				ml.Score = value.Float64
			}
		case moderationlog.FieldExcerpt:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field excerpt", values[i])
			} else if value.Valid {
				ml.Excerpt = value.String
			}
		case moderationlog.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ml.CreatedAt = value.Time
			}
		default:
			ml.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ModerationLog.
// This includes values selected through modifiers, order, etc.
func (ml *ModerationLog) Value(name string) (ent.Value, error) {
	return ml.selectValues.Get(name)
}

// Update returns a builder for updating this ModerationLog.
// Note that you need to call ModerationLog.Unwrap() before calling this method if this ModerationLog
// was returned from a transaction, and the transaction was committed or rolled back.
func (ml *ModerationLog) Update() *ModerationLogUpdateOne {
	return NewModerationLogClient(ml.config).UpdateOne(ml)
}

// Unwrap unwraps the ModerationLog entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ml *ModerationLog) Unwrap() *ModerationLog {
	_tx, ok := ml.config.driver.(*txDriver)
	if !ok {
		panic("ent: ModerationLog is not a transactional entity")
	}
	ml.config.driver = _tx.drv
	return ml
}

// String implements the fmt.Stringer.
func (ml *ModerationLog) String() string {
	var builder strings.Builder
	builder.WriteString("ModerationLog(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ml.ID))
	builder.WriteString("request_id=")
	builder.WriteString(ml.RequestID)
	builder.WriteString(", ")
	builder.WriteString("user_id_snapshot=")
	builder.WriteString(fmt.Sprintf("%v", ml.UserIDSnapshot))
	builder.WriteString(", ")
	builder.WriteString("platform=")
	builder.WriteString(ml.Platform)
	builder.WriteString(", ")
	builder.WriteString("endpoint=")
	builder.WriteString(ml.Endpoint)
	builder.WriteString(", ")
	builder.WriteString("mode=")
	builder.WriteString(ml.Mode)
	builder.WriteString(", ")
	builder.WriteString("flagged=")
	builder.WriteString(fmt.Sprintf("%v", ml.Flagged))
	builder.WriteString(", ")
	builder.WriteString("source=")
	builder.WriteString(ml.Source)
	builder.WriteString(", ")
	builder.WriteString("category=")
	builder.WriteString(ml.Category)
	builder.WriteString(", ")
	builder.WriteString("score=")
	builder.WriteString(fmt.Sprintf("%v", ml.Score))
	builder.WriteString(", ")
	builder.WriteString("excerpt=")
	builder.WriteString(ml.Excerpt)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(ml.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ModerationLogs is a parsable slice of ModerationLog.
type ModerationLogs []*ModerationLog
