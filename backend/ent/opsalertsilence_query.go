// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/DouDOU-start/airgate-core/ent/opsalertsilence"
	"github.com/DouDOU-start/airgate-core/ent/predicate"
)

// OpsAlertSilenceQuery is the builder for querying OpsAlertSilence entities.
type OpsAlertSilenceQuery struct {
	config
	ctx        *QueryContext
	order      []opsalertsilence.OrderOption
	inters     []Interceptor
	predicates []predicate.OpsAlertSilence
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OpsAlertSilenceQuery builder.
func (oasq *OpsAlertSilenceQuery) Where(ps ...predicate.OpsAlertSilence) *OpsAlertSilenceQuery {
	oasq.predicates = append(oasq.predicates, ps...)
	return oasq
}

// Limit the number of records to be returned by this query.
func (oasq *OpsAlertSilenceQuery) Limit(limit int) *OpsAlertSilenceQuery {
	oasq.ctx.Limit = &limit
	return oasq
}

// Offset to start from.
func (oasq *OpsAlertSilenceQuery) Offset(offset int) *OpsAlertSilenceQuery {
	oasq.ctx.Offset = &offset
	return oasq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (oasq *OpsAlertSilenceQuery) Unique(unique bool) *OpsAlertSilenceQuery {
	oasq.ctx.Unique = &unique
	return oasq
}

// Order specifies how the records should be ordered.
func (oasq *OpsAlertSilenceQuery) Order(o ...opsalertsilence.OrderOption) *OpsAlertSilenceQuery {
	oasq.order = append(oasq.order, o...)
	return oasq
}

// First returns the first OpsAlertSilence entity from the query.
// Returns a *NotFoundError when no OpsAlertSilence was found.
func (oasq *OpsAlertSilenceQuery) First(ctx context.Context) (*OpsAlertSilence, error) {
	nodes, err := oasq.Limit(1).All(setContextOp(ctx, oasq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{opsalertsilence.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (oasq *OpsAlertSilenceQuery) FirstX(ctx context.Context) *OpsAlertSilence {
	node, err := oasq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first OpsAlertSilence ID from the query.
// Returns a *NotFoundError when no OpsAlertSilence ID was found.
func (oasq *OpsAlertSilenceQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = oasq.Limit(1).IDs(setContextOp(ctx, oasq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{opsalertsilence.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (oasq *OpsAlertSilenceQuery) FirstIDX(ctx context.Context) int {
	id, err := oasq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single OpsAlertSilence entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one OpsAlertSilence entity is found.
// Returns a *NotFoundError when no OpsAlertSilence entities are found.
func (oasq *OpsAlertSilenceQuery) Only(ctx context.Context) (*OpsAlertSilence, error) {
	nodes, err := oasq.Limit(2).All(setContextOp(ctx, oasq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{opsalertsilence.Label}
	default:
		return nil, &NotSingularError{opsalertsilence.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (oasq *OpsAlertSilenceQuery) OnlyX(ctx context.Context) *OpsAlertSilence {
	node, err := oasq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only OpsAlertSilence ID in the query.
// Returns a *NotSingularError when more than one OpsAlertSilence ID is found.
// Returns a *NotFoundError when no entities are found.
func (oasq *OpsAlertSilenceQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = oasq.Limit(2).IDs(setContextOp(ctx, oasq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{opsalertsilence.Label}
	default:
		err = &NotSingularError{opsalertsilence.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (oasq *OpsAlertSilenceQuery) OnlyIDX(ctx context.Context) int {
	id, err := oasq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OpsAlertSilences.
func (oasq *OpsAlertSilenceQuery) All(ctx context.Context) ([]*OpsAlertSilence, error) {
	ctx = setContextOp(ctx, oasq.ctx, "All")
	if err := oasq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*OpsAlertSilence, *OpsAlertSilenceQuery]()
	return withInterceptors[[]*OpsAlertSilence](ctx, oasq, qr, oasq.inters)
}

// AllX is like All, but panics if an error occurs.
func (oasq *OpsAlertSilenceQuery) AllX(ctx context.Context) []*OpsAlertSilence {
	nodes, err := oasq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of OpsAlertSilence IDs.
func (oasq *OpsAlertSilenceQuery) IDs(ctx context.Context) (ids []int, err error) {
	if oasq.ctx.Unique == nil && oasq.path != nil {
		oasq.Unique(true)
	}
	ctx = setContextOp(ctx, oasq.ctx, "IDs")
	if err = oasq.Select(opsalertsilence.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (oasq *OpsAlertSilenceQuery) IDsX(ctx context.Context) []int {
	ids, err := oasq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (oasq *OpsAlertSilenceQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, oasq.ctx, "Count")
	if err := oasq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, oasq, querierCount[*OpsAlertSilenceQuery](), oasq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (oasq *OpsAlertSilenceQuery) CountX(ctx context.Context) int {
	count, err := oasq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (oasq *OpsAlertSilenceQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, oasq.ctx, "Exist")
	switch _, err := oasq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (oasq *OpsAlertSilenceQuery) ExistX(ctx context.Context) bool {
	exist, err := oasq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OpsAlertSilenceQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (oasq *OpsAlertSilenceQuery) Clone() *OpsAlertSilenceQuery {
	if oasq == nil {
		return nil
	}
	return &OpsAlertSilenceQuery{
		config:     oasq.config,
		ctx:        oasq.ctx.Clone(),
		order:      append([]opsalertsilence.OrderOption{}, oasq.order...),
		inters:     append([]Interceptor{}, oasq.inters...),
		predicates: append([]predicate.OpsAlertSilence{}, oasq.predicates...),
		// clone intermediate query.
		sql:  oasq.sql.Clone(),
		path: oasq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		RuleID int `json:"rule_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.OpsAlertSilence.Query().
//		GroupBy(opsalertsilence.FieldRuleID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (oasq *OpsAlertSilenceQuery) GroupBy(field string, fields ...string) *OpsAlertSilenceGroupBy {
	oasq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &OpsAlertSilenceGroupBy{build: oasq}
	grbuild.flds = &oasq.ctx.Fields
	grbuild.label = opsalertsilence.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		RuleID int `json:"rule_id,omitempty"`
//	}
//
//	client.OpsAlertSilence.Query().
//		Select(opsalertsilence.FieldRuleID).
//		Scan(ctx, &v)
func (oasq *OpsAlertSilenceQuery) Select(fields ...string) *OpsAlertSilenceSelect {
	oasq.ctx.Fields = append(oasq.ctx.Fields, fields...)
	sbuild := &OpsAlertSilenceSelect{OpsAlertSilenceQuery: oasq}
	sbuild.label = opsalertsilence.Label
	sbuild.flds, sbuild.scan = &oasq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a OpsAlertSilenceSelect configured with the given aggregations.
func (oasq *OpsAlertSilenceQuery) Aggregate(fns ...AggregateFunc) *OpsAlertSilenceSelect {
	return oasq.Select().Aggregate(fns...)
}

func (oasq *OpsAlertSilenceQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range oasq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, oasq); err != nil {
				return err
			}
		}
	}
	for _, f := range oasq.ctx.Fields {
		if !opsalertsilence.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if oasq.path != nil {
		prev, err := oasq.path(ctx)
		if err != nil {
			return err
		}
		oasq.sql = prev
	}
	return nil
}

func (oasq *OpsAlertSilenceQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*OpsAlertSilence, error) {
	var (
		nodes = []*OpsAlertSilence{}
		_spec = oasq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*OpsAlertSilence).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &OpsAlertSilence{config: oasq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, oasq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (oasq *OpsAlertSilenceQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := oasq.querySpec()
	_spec.Node.Columns = oasq.ctx.Fields
	if len(oasq.ctx.Fields) > 0 {
		_spec.Unique = oasq.ctx.Unique != nil && *oasq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, oasq.driver, _spec)
}

func (oasq *OpsAlertSilenceQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(opsalertsilence.Table, opsalertsilence.Columns, sqlgraph.NewFieldSpec(opsalertsilence.FieldID, field.TypeInt))
	_spec.From = oasq.sql
	if unique := oasq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if oasq.path != nil {
		_spec.Unique = true
	}
	if fields := oasq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, opsalertsilence.FieldID)
		for i := range fields {
			if fields[i] != opsalertsilence.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := oasq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := oasq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := oasq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := oasq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (oasq *OpsAlertSilenceQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(oasq.driver.Dialect())
	t1 := builder.Table(opsalertsilence.Table)
	columns := oasq.ctx.Fields
	if len(columns) == 0 {
		columns = opsalertsilence.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if oasq.sql != nil {
		selector = oasq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if oasq.ctx.Unique != nil && *oasq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range oasq.predicates {
		p(selector)
	}
	for _, p := range oasq.order {
		p(selector)
	}
	if offset := oasq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := oasq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// OpsAlertSilenceGroupBy is the group-by builder for OpsAlertSilence entities.
type OpsAlertSilenceGroupBy struct {
	selector
	build *OpsAlertSilenceQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (oasgb *OpsAlertSilenceGroupBy) Aggregate(fns ...AggregateFunc) *OpsAlertSilenceGroupBy {
	oasgb.fns = append(oasgb.fns, fns...)
	return oasgb
}

// Scan applies the selector query and scans the result into the given value.
func (oasgb *OpsAlertSilenceGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, oasgb.build.ctx, "GroupBy")
	if err := oasgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OpsAlertSilenceQuery, *OpsAlertSilenceGroupBy](ctx, oasgb.build, oasgb, oasgb.build.inters, v)
}

func (oasgb *OpsAlertSilenceGroupBy) sqlScan(ctx context.Context, root *OpsAlertSilenceQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(oasgb.fns))
	for _, fn := range oasgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*oasgb.flds)+len(oasgb.fns))
		for _, f := range *oasgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*oasgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := oasgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// OpsAlertSilenceSelect is the builder for selecting fields of OpsAlertSilence entities.
type OpsAlertSilenceSelect struct {
	*OpsAlertSilenceQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (oass *OpsAlertSilenceSelect) Aggregate(fns ...AggregateFunc) *OpsAlertSilenceSelect {
	oass.fns = append(oass.fns, fns...)
	return oass
}

// Scan applies the selector query and scans the result into the given value.
func (oass *OpsAlertSilenceSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, oass.ctx, "Select")
	if err := oass.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OpsAlertSilenceQuery, *OpsAlertSilenceSelect](ctx, oass.OpsAlertSilenceQuery, oass, oass.inters, v)
}

func (oass *OpsAlertSilenceSelect) sqlScan(ctx context.Context, root *OpsAlertSilenceQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(oass.fns))
	for _, fn := range oass.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*oass.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := oass.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
