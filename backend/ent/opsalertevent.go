// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/DouDOU-start/airgate-core/ent/opsalertevent"
)

// OpsAlertEvent is the model entity for the OpsAlertEvent schema.
type OpsAlertEvent struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// RuleID holds the value of the "rule_id" field.
	RuleID int `json:"rule_id,omitempty"`
	// RuleName holds the value of the "rule_name" field.
	RuleName string `json:"rule_name,omitempty"`
	// Metric holds the value of the "metric" field.
	Metric string `json:"metric,omitempty"`
	// Operator holds the value of the "operator" field.
	Operator string `json:"operator,omitempty"`
	// Value holds the value of the "value" field.
	Value float64 `json:"value,omitempty"`
	// Threshold holds the value of the "threshold" field.
	Threshold float64 `json:"threshold,omitempty"`
	// Severity holds the value of the "severity" field.
	Severity string `json:"severity,omitempty"`
	// firing / resolved
	Status string `json:"status,omitempty"`
	// Message holds the value of the "message" field.
	Message string `json:"message,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// ResolvedAt holds the value of the "resolved_at" field.
	ResolvedAt   *time.Time `json:"resolved_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*OpsAlertEvent) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case opsalertevent.FieldValue, opsalertevent.FieldThreshold:
			values[i] = new(sql.NullFloat64)
		case opsalertevent.FieldID, opsalertevent.FieldRuleID:
			values[i] = new(sql.NullInt64)
		case opsalertevent.FieldRuleName, opsalertevent.FieldMetric, opsalertevent.FieldOperator, opsalertevent.FieldSeverity, opsalertevent.FieldStatus, opsalertevent.FieldMessage:
			values[i] = new(sql.NullString)
		case opsalertevent.FieldCreatedAt, opsalertevent.FieldResolvedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the OpsAlertEvent fields.
func (oae *OpsAlertEvent) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case opsalertevent.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			oae.ID = int(value.Int64)
		case opsalertevent.FieldRuleID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field rule_id", values[i])
			} else if value.Valid {
				oae.RuleID = int(value.Int64)
			}
		case opsalertevent.FieldRuleName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field rule_name", values[i])
			} else if value.Valid {
				oae.RuleName = value.String
			}
		case opsalertevent.FieldMetric:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field metric", values[i])
			} else if value.Valid {
				oae.Metric = value.String
			}
		case opsalertevent.FieldOperator:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field operator", values[i])
			} else if value.Valid {
				oae.Operator = value.String
			}
		case opsalertevent.FieldValue:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field value", values[i])
			} else if value.Valid {
				oae.Value = value.Float64
			}
		case opsalertevent.FieldThreshold:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field threshold", values[i])
			} else if value.Valid {
				oae.Threshold = value.Float64
			}
		case opsalertevent.FieldSeverity:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field severity", values[i])
			} else if value.Valid {
				oae.Severity = value.String
			}
		case opsalertevent.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				oae.Status = value.String
			}
		case opsalertevent.FieldMessage:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field message", values[i])
			} else if value.Valid {
				oae.Message = value.String
			}
		case opsalertevent.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				oae.CreatedAt = value.Time
			}
		case opsalertevent.FieldResolvedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field resolved_at", values[i])
			} else if value.Valid {
				oae.ResolvedAt = new(time.Time)
				*oae.ResolvedAt = value.Time
			}
		default:
			oae.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// GetValue returns the ent.Value that was dynamically selected and assigned to the OpsAlertEvent.
// This includes values selected through modifiers, order, etc.
func (oae *OpsAlertEvent) GetValue(name string) (ent.Value, error) {
	return oae.selectValues.Get(name)
}

// Update returns a builder for updating this OpsAlertEvent.
// Note that you need to call OpsAlertEvent.Unwrap() before calling this method if this OpsAlertEvent
// was returned from a transaction, and the transaction was committed or rolled back.
func (oae *OpsAlertEvent) Update() *OpsAlertEventUpdateOne {
	return NewOpsAlertEventClient(oae.config).UpdateOne(oae)
}

// Unwrap unwraps the OpsAlertEvent entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (oae *OpsAlertEvent) Unwrap() *OpsAlertEvent {
	_tx, ok := oae.config.driver.(*txDriver)
	if !ok {
		panic("ent: OpsAlertEvent is not a transactional entity")
	}
	oae.config.driver = _tx.drv
	return oae
}

// String implements the fmt.Stringer.
func (oae *OpsAlertEvent) String() string {
	var builder strings.Builder
	builder.WriteString("OpsAlertEvent(")
	builder.WriteString(fmt.Sprintf("id=%v, ", oae.ID))
	builder.WriteString("rule_id=")
	builder.WriteString(fmt.Sprintf("%v", oae.RuleID))
	builder.WriteString(", ")
	builder.WriteString("rule_name=")
	builder.WriteString(oae.RuleName)
	builder.WriteString(", ")
	builder.WriteString("metric=")
	builder.WriteString(oae.Metric)
	builder.WriteString(", ")
	builder.WriteString("operator=")
	builder.WriteString(oae.Operator)
	builder.WriteString(", ")
	builder.WriteString("value=")
	builder.WriteString(fmt.Sprintf("%v", oae.Value))
	builder.WriteString(", ")
	builder.WriteString("threshold=")
	builder.WriteString(fmt.Sprintf("%v", oae.Threshold))
	builder.WriteString(", ")
	builder.WriteString("severity=")
	builder.WriteString(oae.Severity)
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(oae.Status)
	builder.WriteString(", ")
	builder.WriteString("message=")
	builder.WriteString(oae.Message)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(oae.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := oae.ResolvedAt; v != nil {
		builder.WriteString("resolved_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteByte(')')
	return builder.String()
}

// OpsAlertEvents is a parsable slice of OpsAlertEvent.
type OpsAlertEvents []*OpsAlertEvent
